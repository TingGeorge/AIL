# ALL in life — Context

單一 bounded context。本檔只是詞彙表，不放實作細節。

## 詞彙表

- **生活需求（Need）**：使用者想完成的事情，例如晚餐、採買、找活動或找免費資源。
- **硬限制（Hard constraint）**：不符合就不能推薦的條件，例如預算上限、人數、日期、區域、最大距離／時間或資格。
- **食品份量（Food portion）**：食品明示可供應的人數或份數；需求 N 是上限，明確區間以最大值比較，已知 1..N 可成為候選並以 N、N−1…排序，超過 N 是硬限制不符。沒有明示份量就維持待確認；件數與重量不等於份量。
- **軟偏好（Soft preference）**：符合硬限制後用來排序或標籤的偏好，例如可外帶、品牌、時段或便利性。
- **需求與限制**：需求解析的結構化輸出：一個生活需求，加上它的硬限制與軟偏好。搜尋以它為輸入。
- **需求解析**：把使用者的自然語言（逐字稿或文字）轉成需求與限制的步驟。不是 Agent。
- **語音輸入（Voice input）**：使用者用說的提供需求與限制的管道。它把語音變成逐字稿後交給需求解析；不是 Agent。
- **逐字稿（Transcript）**：從使用者語音辨識出的原文。搜尋前一定原樣顯示給使用者。
- **修正語句（Correction delta）**：使用者對既有需求與限制說或打的一句更正，只更新被提到的欄位，其餘不變。
- **未解析內容（Unresolved）**：逐字稿裡聽到但對不上任何欄位的片語。顯示給使用者，不猜、不補值。
- **生活選項（Option）**：一個可被使用者採取行動的食品、商品、資源、活動或交通服務。
- **候選紀錄（Candidate record）**：包含選項、比較欄位、來源與證據的結構化資料單位。
- **資料證據（Evidence）**：支持價格、費用、資格、時間、地點或可用性等關鍵欄位的來源片段、頁面或官方公告。
- **比較基準（Baseline）**：用來計算節省的已命名方案，例如使用者原方案、在地常見方案或使用者選擇可用的 Costco 基準。
- **推薦結果（Recommendation）**：通過第一階段篩選後，經推薦排序並附推薦理由呈現給使用者的候選紀錄。
- **直接成本（Direct cost）**：使用者為取得選項必須支付的價格，單位為新台幣（TWD）。
- **總可比成本（Comparable total cost）**：價格加上必要費用，扣除明確適用的折扣；不把未知費用當成零。
- **免費資源（Free resource）**：來源明確表示直接費用為零的選項；其他資格、登記、會員或押金條件仍須揭露。
- **資料狀態（Data status）**：已驗證、部分驗證／待確認、過期、衝突或無法納入比較。
- **待確認候選（Pending candidate）**：因必要資料或限制證據不足，尚不能確認符合或不符合本次需求的候選紀錄；呈現時位於主要候選之後。
- **固定區域（Fixed area）**：本 MVP 唯一展示範圍「圓山區」；不是使用者的精確住址。
- **第一階段篩選（Filter stage）**：對已匯入的候選紀錄套用證據閘門與硬限制的 deterministic 步驟，整個搜尋只跑一次。不是 Agent。
- **推薦排序（Recommendation ranking）**：對通過第一階段篩選的候選紀錄決定預設呈現順序的步驟；不改變候選資格。食品有份量需求時先比較份量接近度；生存模式先排免費資源，再於其中套用份量順序，其餘才比較總可比成本與軟偏好。
- **手動排序（Manual sort）**：使用者明確選擇成本、距離或資料日期作為呈現順序；它取代推薦順序，但不改變主要、待確認或已排除的分類。
- **推薦理由（Reason）**：推薦排序為每筆候選紀錄產生的一句說明，解釋它為何排在這個位置。
- **Agent**：負責一種推薦排序任務的 LLM 執行單元；不上網搜尋，只處理資料庫裡已匯入的候選紀錄。只有兩個：付費選項 Agent 與免費資源 Agent。需求解析、語音輸入、第一階段篩選與正規化都不是 Agent。
- **資料匯入（Ingestion）**：在搜尋之前，由爬蟲或 API 腳本把候選紀錄、來源與證據寫進資料庫的作業。_Avoid_：即時搜尋、網路搜尋 Agent。
- **估算距離（Estimated distance）**：使用者當下位置到選項地點的直線距離；只在使用者同意定位時計算，不保存。沒有估算距離的選項照常顯示，只是不做距離篩選。
- **使用者（User）**：使用 ALL in life 的人，可以是匿名使用者或已登入使用者。
- **匿名使用者（Anonymous user）**：未建立或未使用帳號，仍可完成需求解析、搜尋與修改設定的人；收藏與清單需要帳號。
- **帳號（Account）**：可被使用者登入，並擁有清單、收藏與設定等帳號資料的持久身份。
- **登入識別（Login identity）**：用來辨識帳號的唯一 username；它與可修改的顯示名稱分開。
- **工作階段（Session）**：一次匿名或已登入的使用期間；登入工作階段由可逾時或撤銷的身份憑證識別。
- **帳號私有資料（Account-owned data）**：只屬於單一帳號的清單、收藏與設定。
- **清單（List）**：使用者以愛心保存、準備後續查看或標記已買的帳號私有集合；與收藏彼此獨立。
- **收藏（Favorites）**：使用者以書籤保存的帳號私有集合；與清單彼此獨立。
- **共享資料（Shared data）**：可被多位使用者看見或共同影響的資料，例如回報與團購加入狀態。
- **支援式帳號恢復（Support-assisted account recovery）**：使用者忘記密碼時，透過平台支援管道尋求協助，而不是在 App 內自動重設密碼。
- **設定（Settings）**：帳號或匿名工作階段的偏好集合：每月預算、已花費、生存模式、排除項目、偏好與 Costco 基準是否可用。
- **生存模式（Survival mode）**：使用者開啟的設定；在推薦順序中免費資源排在付費選項之前，不覆蓋使用者明確選擇的手動排序基準。
- **每月預算／已花費（Monthly budget / Spent）**：使用者自填的當月可用金額與已用金額；剩餘額在沒有說預算時作為預算上限的預設值，並在確認畫面標示「來自設定」。
- **排除項目（Exclusion）**：使用者不要的成分或類型標籤，例如牛、海鮮、辣；是硬限制。
- **回報（Report）**：使用者對一筆候選紀錄提交的共享紀錄，需要帳號。分為資料回報與體驗回報。回報不自動改變資料狀態。
- **資料回報（Data report）**：指出候選紀錄資料錯誤的回報：價格過期、條件錯誤、來源失效或分類錯誤。
- **體驗回報（Experience report）**：使用者對自身經驗的回報：食安、過敏或身體不適。它是使用者陳述，不是系統判斷。
- **團體優惠（Group offer）**：商家或官方提供、需湊滿指定人數才可使用的優惠條件與兌換碼；實際預約、購買、付款與核銷仍由提供者處理。
- **兌換碼（Redeem code）**：商家或官方提供、用來兌換團體優惠的代碼；它不是 App 的加入識別碼。
- **團購加入（Group join）**：已登入使用者對一筆團體優惠占用一個 App 內名額；同一帳號對同一優惠最多一個名額，可以退出。
- **目前團（Current group）**：每筆候選紀錄目前只有一個共享團，`min_people` 同時作為成團門檻與本版名額上限；達門檻即顯示已成團，不自動建立下一團。
- **揪團（Team-up）**：公開顯示人數進度；加入、退出與成員身分需要帳號。只有已加入同一團的使用者能看該團成員的 nickname 與 username；平台不代為報名、下單或付款。
