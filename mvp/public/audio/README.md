# ALL IN LIFE background music

`all-in-life-light-theme.wav` is one original 32-second light ambient loop generated entirely by this repository. It uses no third-party samples.

Run `npm run music:generate` from `mvp/` to rebuild it deterministically.
